@model JsonCMS.Models.Core.SocialNetworks
<ul class="socialIcons">
    @foreach (var social in Model.social)
    {
        <li>
            <a href="@social.url" target="@social.target"><i alt="@social.altText" title="@social.altText" class="fa @social.icon fa-lg"></i></a>
        </li>
    }
</ul>